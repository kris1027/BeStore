import type { NavItem } from "./nav";

// Storefront header links. Empty until a feature adds its entry (for example a category page);
// the mobile menu button stays hidden while it is empty.
export const storeNavItems: readonly NavItem[] = [];
